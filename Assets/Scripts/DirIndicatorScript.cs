using System;
using UnityEngine;

public class DirIndicatorScript : MonoBehaviour
{
        public Transform followTarget;
        private void Update()
        {
                if (followTarget)
                {
                        //transform.position = followTarget.position;
                }
        }
}
